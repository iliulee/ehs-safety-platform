# 安全管理平台 V5.0 改造 — 开发会话总结

> 溜哥，这是你换账号前需要知道的所有信息。新账号登录后，开 Code 任务，粘贴对应模块的提示词即可。

---

## 当前项目状态

| 项目 | 状态 |
|------|------|
| 代码仓库 | `F:\安全管理平台` |
| Git 提交 | 9 次，完好 |
| 模块 1（分词） | ✅ 已完成（92a37f3） |
| 模块 2（AI 调用） | ✅ 已完成（6480082） |
| 模块 3（文本切分） | ✅ 已完成（610b1e8） |
| 模块 4（全文检索） | ✅ 已完成（5d4686a） |
| 已知 Bug | 2 个已修复，1 个待验证 |

Git 提交记录：
```
5d4686a v5.0 模块4：全文检索替换为 MiniSearch（BM25+模糊+前缀+建议）
610b1e8 v5.0 模块3：文本切分切换为 @langchain/textsplitters (RecursiveCharacterTextSplitter)
6480082 v5.0 模块2：AI 调用替换为 OpenAI SDK（DeepSeek）
92a37f3 v5.0 模块1：中文分词切换为 @node-rs/jieba
33cb22b docs: 记录 3 个已知待修 Bug，v5.0 改造期间禁止碰模板库区域
94d3679 docs: 更新依赖状态标记为已安装
9c0036f v5.0 依赖安装：MiniSearch + OpenAI SDK + LangChain + jieba
f99ec76 v4.1.3 快照：v5.0 改造前备份
```

---

## 换账号后操作步骤

1. 退出当前 TRAE 账号，登录新账号
2. 切到 **Code 模式**
3. 新建任务，确认 3 项：
   - 运行环境：**本地**（不要选云端沙箱）
   - 工作目录：**`F:\安全管理平台`**（不要带 `\安全软件V2.0`）
   - 弹窗问"沙箱外运行"：点 **是**
4. 粘贴下面对应模块的提示词，发送

---

## 核心规范文件（新会话自动加载）

- `AGENTS.md` — 在 `F:\安全管理平台\安全软件V2.0\` 下，Trae 自动加载
- `PROJECT-new_SPEC.md` — 产品需求和技术方案
- `VIBE_CODING_PROMPT.md` — 编码规范、拼好码原则、质量门禁

---

## 模块 3 提示词（文本切分）✅ 已完成 — commit 610b1e8

> 以下为原始提示词，保留供参考。

```
## 上下文

AGENTS.md 已加载完整规范。当前状态：
- Git：F:\安全管理平台，已有 7 次提交
- 模块 1（分词）✅ 模块 2（AI 调用）✅
- 4 个新包已安装：minisearch / openai / @langchain/textsplitters / @node-rs/jieba

## 任务：模块 3 — 文本切分替换

### 目标
把 src/services/chunker.service.ts 的自研切分换成 @langchain/textsplitters 的 RecursiveCharacterTextSplitter。

### 具体操作
1. 先读 src/services/chunker.service.ts 理解现有逻辑
2. 创建 src/services/chunker.adapter.ts，用 RecursiveCharacterTextSplitter 替换自研切分，配置中文分隔符
3. 保持对外接口不变
4. 旧代码重命名为 chunker.service.old.ts，不要删除
5. 更新调用方的 import 路径（如需要），不改业务逻辑

### 验证
- npx tsc --noEmit 零错误
- npx vite build 必须通过
- 通过后 git commit

### 关键约束
- 禁止重写：docxtemplater / ExcelJS / Dexie.js / shadcn/ui / MiniSearch / mammoth / pdfjs-dist
- 不改 UI 组件，不改页面，不碰 src/pages/templates/
- 遇到不确定的，停手问我
```

---

## 模块 4 提示词（全文检索）✅ 已完成 — commit 5d4686a

> 以下为原始提示词，保留供参考。

```
## 上下文

AGENTS.md 已加载完整规范。当前状态：
- Git：F:\安全管理平台，已有 8 次提交
- 模块 1（分词）✅ 模块 2（AI 调用）✅ 模块 3（文本切分）✅
- 4 个新包已安装：minisearch / openai / @langchain/textsplitters / @node-rs/jieba

## 任务：模块 4 — 全文检索替换

### 目标
把 src/services/bm25.service.ts 和 src/services/knowledge-retrieval.service.ts 的自研检索换成 MiniSearch。

### 具体操作
1. 先读 src/services/bm25.service.ts 和 src/services/knowledge-retrieval.service.ts 理解现有逻辑
2. 创建 src/services/mini-search.adapter.ts，用 MiniSearch 替换自研 BM25：
   - 配置 MiniSearch 的 fields、searchOptions（BM25、fuzzy、prefix）
   - 用现有的 tokenizer.adapter.ts（@node-rs/jieba）作为 MiniSearch 的 tokenize 函数
   - 实现索引构建（从 Dexie 读取文档）、搜索、suggest 方法
3. 更新 knowledge-retrieval.service.ts 的 import 和调用
4. 旧代码重命名为 bm25.service.old.ts，不要删除
5. 保持对外接口不变

### 验证
- npx tsc --noEmit 零错误
- npx vite build 必须通过
- 通过后 git commit

### 关键约束
- 禁止重写：docxtemplater / ExcelJS / Dexie.js / shadcn/ui / MiniSearch / mammoth / pdfjs-dist
- 不改 UI 组件，不改页面，不碰 src/pages/templates/
- MiniSearch 的 tokenize 必须用已有的 tokenizer.adapter.ts，不要另起炉灶
- 遇到不确定的，停手问我
```

---

## 已知待修 Bug

| # | Bug | 涉及文件 | 状态 |
|---|-----|---------|------|
| 1 | 模板库新建分类不显示 | `src/pages/templates/`、`src/db/` | ✅ 已修复（并发API竞态条件，加 guard flag 确保单次执行） |
| 2 | 模板变量只能输入一个字 | `src/pages/templates/components/` | ✅ 已修复（react-virtualizer 引用不稳定，用 useMemo/useCallback 稳定 getScrollElement 和 estimateSize） |
| 3 | Word 加载一直转圈 | `src/pages/templates/`、`src/services/templateService.ts` | ⏳ 待验证 |

---

## 核心规则速记

- 能复用时不重造 → 先查 package.json，再查 npm/GitHub
- 写完必须跑 `npx tsc --noEmit && npx vite build`，零错误才算过
- 禁止重写：docxtemplater / ExcelJS / Dexie.js / shadcn/ui / MiniSearch / mammoth / pdfjs-dist
- 旧代码不删，改为 `.old.ts` 备份
- 每个模块改完一定 git commit